# 禁止从上级目录继承配置（强烈建议开启）
config.stopBubbling = true
# 让 @ToString(includeSuper=true) 默认调用父类的 toString()
lombok.tostring.callsuper=CALL
# 让 @EqualsAndHashCode(callSuper=true) 默认调用父类的 equals/hashCode()
lombok.equalsandhashcode.callsuper=CALL
# 开启链式调用：实体对象的 setter 返回 this
lombok.accessors.chain=true
